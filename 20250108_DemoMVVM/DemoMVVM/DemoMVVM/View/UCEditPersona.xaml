<UserControl
    x:Class="DemoMVVM.View.UCEditPersona"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DemoMVVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:transformers="using:DemoMVVM.View.Transformers"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="root">

    <UserControl.Resources>
        <transformers:SexeRadioTransform x:Key="sexeRadioTransform"></transformers:SexeRadioTransform>
    </UserControl.Resources>

    <Grid ColumnSpacing="5" RowSpacing="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>


        <TextBlock Text="Nom:" Grid.Column="0" Grid.Row="0"/>
        <TextBox Text="{Binding ElementName=root, Path=LaPersona.Nom, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged }" Grid.Column="1" Grid.Row="0" />

        <TextBlock Text="Edat:" Grid.Column="0" Grid.Row="1"/>
        <TextBox Text="{Binding ElementName=root, Path=LaPersona.Edat, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged }" Grid.Column="1" Grid.Row="1" />

        <TextBlock Text="Actiu:" Grid.Column="0" Grid.Row="2"/>
        <CheckBox IsChecked="{Binding ElementName=root, Path=LaPersona.Actiu, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged }" Grid.Column="1" Grid.Row="2" />

        <TextBlock Text="Sexe:" Grid.Column="0" Grid.Row="3" />
        <ComboBox 
            SelectedItem="{Binding ElementName=root, Path=LaPersona.Sexe, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
            ItemsSource="{Binding ElementName=root, Path=LaPersona.ListSexe}" 
            Grid.Column="1" Grid.Row="3"></ComboBox>

        <TextBlock Text="Sexe:" Grid.Column="0" Grid.Row="4" />
        <StackPanel Grid.Column="1" Grid.Row="4" Orientation="Horizontal">
            <RadioButton IsChecked="{Binding ElementName=root, Path=LaPersona.Sexe, Converter={StaticResource sexeRadioTransform}, ConverterParameter=0}">Done</RadioButton>
            <RadioButton IsChecked="{Binding ElementName=root, Path=LaPersona.Sexe, Converter={StaticResource sexeRadioTransform}, ConverterParameter=1}">Home</RadioButton>
            <RadioButton IsChecked="{Binding ElementName=root, Path=LaPersona.Sexe, Converter={StaticResource sexeRadioTransform}, ConverterParameter=2}">No definit</RadioButton>
        </StackPanel>

        <TextBlock Text="Image:" Grid.Column="0" Grid.Row="5" />
        <Image Source="{Binding ElementName=root, Path=LaPersona.ImageURL, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged }" Grid.Column="1" Grid.Row="5"/>

        

    </Grid>
</UserControl>
